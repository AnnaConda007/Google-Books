export enum SortType {
  RELEVANCE = "relevance",
  NEWEST = "newest",
}

export enum BookCategories {
  ALL = "All",
  ART = "Art",
  BIOGRAPHY = "Biography",
  COMPUTERS = "Computers",
  HISTORY = "History",
  MEDICAL = "Medical",
  POETRY = "Poetry",
}
